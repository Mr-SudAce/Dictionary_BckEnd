"""Deprecated API routes.

This module is retained for reference but its urlpatterns are no
longer included in the project.  The REST API is now served via a
router in `api.urls` using viewsets (see `api/api_views.py`).
"""

urlpatterns = []
